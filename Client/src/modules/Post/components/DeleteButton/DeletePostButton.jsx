import Button from "../../../../UI/Button/Button.jsx";

const DeletePostButton = ({...props}) => {
    return (
        <Button {...props}>
            Delete
        </Button>
    );
};

export default DeletePostButton;